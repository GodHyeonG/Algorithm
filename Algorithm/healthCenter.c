#include <stdio.h>
void main()
{
    int center, intersection; //Each number of center, intersection
    int exist_i; //Intersection at which existing center is found
    int i1, i2, len; //length: length of road segment connecting intersections
    for (int i = 0; i < intersection; i++)
    {
        
    }
}